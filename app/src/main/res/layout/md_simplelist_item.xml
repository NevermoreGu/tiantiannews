<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?selectableItemBackground"
    android:focusable="true"
    android:gravity="start|center_vertical"
    android:minHeight="@dimen/offset_56"
    android:orientation="horizontal"
    android:paddingEnd="@dimen/offset_24"
    android:paddingLeft="@dimen/offset_24"
    android:paddingRight="@dimen/offset_24"
    android:paddingStart="@dimen/offset_24">

    <ImageView
        android:id="@+id/icon"
        android:layout_width="@dimen/offset_40"
        android:layout_height="@dimen/offset_40"
        android:layout_gravity="start|center_vertical"
        android:layout_marginEnd="@dimen/offset_16"
        android:layout_marginRight="@dimen/offset_16"
        android:scaleType="fitXY"
        tools:background="#f5f5f5"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:textSize="@dimen/font_size_18"
        tools:text="Title" />

</LinearLayout>