<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tiantiannews">

    <!-- 用于访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 写入扩展存储，向扩展卡写入数据，用于写入缓存定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 音频操作权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- 用于访问wifi网络信息，wifi信息会用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- 用于访问GPS定位 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- 这个权限用于获取wifi的获取权限，wifi信息会用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 监听系统开机广播权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_LOGS" />

    <application
        android:name=".DaysApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 高德地图 -->
        <!-- 设置key -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="dc7b88c62279b506a52e8cf60b09e1f3" />
        <!-- 定位需要的服务 -->
        <service android:name="com.amap.api.location.APSService" />
        <!-- 高德地图 -->
        <activity android:name=".ui.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.MainActivity" />
        <activity android:name=".ui.activity.LoginActivity" />
        <activity android:name=".ui.activity.PicturesBrowseActivity"
            android:process=":img"/>
        <activity
            android:name=".ui.activity.SelectPicturesActivity"
            android:process=":img" />
        <activity android:name=".ui.activity.ForgetPasswordActivity" />
        <activity android:name=".ui.activity.ScanActivity" />

        <provider
            android:name=".data.contentprovider.CitiesDBProvider"
            android:authorities="com.guqian.database.cities"
            android:exported="false" />

        <activity
            android:name=".ui.activity.CitiesActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".ui.activity.TestActivity" />
        <activity android:name=".ui.activity.StudentMessageDetailsActivity" />
        <activity android:name=".ui.activity.ImageViewActivity" />

        <receiver android:name=".broadcastreceiver.ActionBootCompletedBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".broadcastreceiver.AlarmNotificationBroadcastReceiver" />

        <activity android:name=".ui.activity.TestProgressActivity" />
        <activity android:name=".ui.activity.TestStatusBarActivity" />
        <activity android:name=".ui.activity.TestHandlerActivity" />
        <activity android:name=".ui.activity.PersonInfoActivity" />
        <activity android:name=".ui.activity.HeadIconActivity" />

        <service
            android:name=".service.ImageService"
            android:process=":img"
            android:enabled="true"
            android:exported="true"></service>
        <activity android:name=".ui.activity.ContinuousCaptureActivity"></activity>
    </application>

</manifest>